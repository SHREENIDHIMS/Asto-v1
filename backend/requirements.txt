fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
psycopg[binary,pool]==3.3.4
psycopg-pool==3.3.1
pgvector==0.5.0
PyJWT==2.13.0
fastembed==0.8.0
onnxruntime==1.28.0
RapidFuzz==3.14.5
python-multipart==0.0.32
numpy==2.4.6
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- Extractive summarization (response/summarizer.py) ---
sumy==0.11.0

# --- Admin 2FA (H4: TOTP) ---
# cryptography is a direct import in app/auth/totp.py (Fernet) — pin it
# explicitly rather than relying on the pdfminer.six transitive dep.
pyotp==2.9.0
cryptography==50.0.0

# --- Batch ingestion optional dependencies ---
pdfplumber==0.11.6
python-docx==1.2.0
# OCR fallback for scanned PDFs (backend/app/documents/ocr.py)
pytesseract==0.3.13
Pillow==11.3.0
pdf2image==1.17.0
# Additional document format extraction (backend/app/documents/text_extraction.py)
openpyxl==3.1.5
python-pptx==1.0.2
striprtf==0.0.28

# --- Client-facing watermark (I5: app/documents/watermark.py) ---
# pypdf merges a reportlab-generated text page onto each PDF page.
pypdf==6.16.1
reportlab==5.0.0
