name: CI

on:
  push:
    branches: [main, "feature/**"]
  pull_request:
    branches: [main]

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: admin
          POSTGRES_PASSWORD: adminpass
          POSTGRES_DB: asto_assistant
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U admin"
          --health-interval "10s"
          --health-timeout "5s"
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: backend/requirements.txt

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt -r backend/requirements-dev.txt

      - name: Install pgvector extension
        env:
          PGPASSWORD: adminpass
        run: |
          psql -h 127.0.0.1 -U admin -d asto_assistant -c "CREATE EXTENSION IF NOT EXISTS vector;"

      - name: Pre-download embedding model
        working-directory: backend
        run: |
          python -c "
          from app.config import settings
          from fastembed import TextEmbedding
          cache_dir = settings.embedding_cache_dir
          TextEmbedding(
              model_name=settings.embedding_model,
              cache_dir=cache_dir,
          ).embed(['ci warmup'])
          list(TextEmbedding(
              model_name=settings.embedding_model,
              cache_dir=cache_dir,
              local_files_only=True,
          ).embed(['ci warmup']))
          print('embedding model cached and loadable via local_files_only at', cache_dir)
          "

      - name: Install frontend dependencies
        working-directory: frontend
        run: |
          npm ci

      - name: Build frontend
        working-directory: frontend
        run: |
          npm run build

      - name: Lint frontend
        working-directory: frontend
        run: |
          npm run lint

      - name: Typecheck frontend
        working-directory: frontend
        run: |
          npm run typecheck

      - name: Run frontend tests
        working-directory: frontend
        run: |
          npm test

      - name: Install Playwright chromium
        working-directory: frontend
        run: |
          npx playwright install --with-deps chromium

      - name: Run frontend a11y audit
        working-directory: frontend
        run: |
          npm run a11y

      - name: Run unit tests
        env:
          ASTO_DATABASE_URL: postgresql://admin:adminpass@127.0.0.1:5432/asto_assistant
          ASTO_JWT_SECRET: dev-only-secret-change-me-in-production-32chars
          ASTO_ENVIRONMENT: test
          PGPASSWORD: adminpass
        run: |
          cd backend
          python -m pytest tests/unit/ -v --timeout=60

      - name: Run integration tests
        env:
          ASTO_DATABASE_URL: postgresql://admin:adminpass@127.0.0.1:5432/asto_assistant
          ASTO_JWT_SECRET: dev-only-secret-change-me-in-production-32chars
          ASTO_ENVIRONMENT: test
          PGPASSWORD: adminpass
          PYTHONPATH: ${{ github.workspace }}
        run: |
          cd backend
          python -m pytest tests/integration/ -v --timeout=120

  benchmark:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    timeout-minutes: 30

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: admin
          POSTGRES_PASSWORD: adminpass
          POSTGRES_DB: asto_assistant
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U admin"
          --health-interval "10s"
          --health-timeout "5s"
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt
          pip install pytest
      - name: Install pgvector extension
        env:
          PGPASSWORD: adminpass
        run: |
          psql -h 127.0.0.1 -U admin -d asto_assistant -c "CREATE EXTENSION IF NOT EXISTS vector;"
      - name: Pre-download embedding model
        working-directory: backend
        env:
          ASTO_EMBEDDING_CACHE_DIR: ${{ github.workspace }}/backend/nlp_models/embeddings
        run: |
          python -c "
          from app.config import settings
          from fastembed import TextEmbedding
          cache_dir = settings.embedding_cache_dir
          TextEmbedding(
              model_name=settings.embedding_model,
              cache_dir=cache_dir,
          ).embed(['ci warmup'])
          list(TextEmbedding(
              model_name=settings.embedding_model,
              cache_dir=cache_dir,
              local_files_only=True,
          ).embed(['ci warmup']))
          print('embedding model cached and loadable via local_files_only at', cache_dir)
          "
      - name: Ensure database schema
        working-directory: backend
        env:
          ASTO_DATABASE_URL: postgresql://admin:adminpass@127.0.0.1:5432/asto_assistant
        run: |
          python -c "from app.db.postgres.schema import ensure_schema; ensure_schema()"
      - name: Run evaluation benchmark
        env:
          ASTO_DATABASE_URL: postgresql://admin:adminpass@127.0.0.1:5432/asto_assistant
          ASTO_EMBEDDING_CACHE_DIR: ${{ github.workspace }}/backend/nlp_models/embeddings
          PGPASSWORD: adminpass
        run: |
          python -m evaluation.run_benchmark --output-dir evaluation/reports
      - name: Upload benchmark report
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-report
          path: evaluation/reports/
